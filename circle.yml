machine:
  node:
    version: 4.2.4

dependencies:
  pre:
    - sh bin/check-libstdc-deb
  post:
    - npm run compile
    - npm run compile-dev

test:
  override:
    - npm run test-cov
  post:
    - npm run vile
